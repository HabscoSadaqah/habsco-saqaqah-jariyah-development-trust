const SUPABASE_URL='https://ythnoeyxovapydbmymdo.supabase.co';
const SUPABASE_PUBLISHABLE_KEY='sb_publishable_nfSR2tMCFuHCpkOjjNIakw_P85zunsN';
const supabaseClient=window.supabase.createClient(SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY);
const $=id=>document.getElementById(id);const money=n=>new Intl.NumberFormat('en-NG',{style:'currency',currency:'NGN',minimumFractionDigits:2}).format(Number(n||0));
function show(id,text){const el=$(id);if(!el)return;el.textContent=text;el.classList.add('show');}
function esc(v){return String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));}
let members=[];let loading=false;let currentSession=null;
async function guard(){const {data:{session},error:sessionError}=await supabaseClient.auth.getSession();if(sessionError||!session){location.href='auth.html';return null}const {data:profile,error}=await supabaseClient.from('profiles').select('full_name,role,status').eq('id',session.user.id).single();if(error||profile?.role!=='admin'||profile?.status!=='active'){$('adminMeta').textContent='Administrator access required.';setTimeout(()=>location.href='member.html',1200);return null}$('adminMeta').textContent=`Signed in as ${profile.full_name||session.user.email} · Administrator`;$('heroAdminName').textContent=profile.full_name||'Administrator';$('adminName').textContent=profile.full_name||'Administrator';$('adminEmail').textContent=session.user.email||'Admin account';$('app').classList.remove('hidden');return session;}
function loadingRow(id,cols){$(id).innerHTML=`<tr><td colspan="${cols}">Loading…</td></tr>`;}
async function load(){if(loading)return;loading=true;try{loadingRow('membersTable',6);loadingRow('fundingTable',6);loadingRow('qardTable',5);loadingRow('approvedQardTable',4);loadingRow('repaymentTable',5);loadingRow('auditTable',4);const [pRes,wRes,fRes,qRes,rRes,aRes,cRes]=await Promise.all([supabaseClient.from('profiles').select('id,full_name,member_id,status,role,created_at').order('created_at',{ascending:false}),supabaseClient.from('wallets').select('user_id,balance'),supabaseClient.from('funding_requests').select('id,user_id,amount,paid_date,payment_reference,purpose,status,created_at').eq('status','pending').order('created_at',{ascending:false}),supabaseClient.from('qard_requests').select('id,user_id,amount,repayment_plan,purpose,status,created_at').eq('status','pending').order('created_at',{ascending:false}),supabaseClient.from('qard_requests').select('id,user_id,amount,purpose,status,created_at').eq('status','approved').order('created_at',{ascending:false}),supabaseClient.from('qard_repayment_requests').select('id,user_id,amount,payment_reference,paid_date,status,created_at').eq('status','pending').order('created_at',{ascending:false}),supabaseClient.from('audit_logs').select('action,target_user_id,reference_id,details,created_at').order('created_at',{ascending:false}).limit(25),supabaseClient.from('cooperative_wallets').select('balance,currency').eq('id',1).single()]);const errors=[pRes,wRes,fRes,qRes,rRes,aRes,cRes].filter(x=>x.error);if(errors.length)show('globalMsg',`Some dashboard data could not load: ${errors[0].error.message}`);members=pRes.data||[];const names=Object.fromEntries(members.map(m=>[m.id,m.full_name||m.member_id]));const bal=Object.fromEntries((wRes.data||[]).map(w=>[w.user_id,w.balance]));$('membersCount').textContent=members.filter(m=>m.status==='active').length;$('fundingCount').textContent=(fRes.data||[]).length;$('qardCount').textContent=(qRes.data||[]).length;$('totalBalance').textContent=money((wRes.data||[]).reduce((s,w)=>s+Number(w.balance||0),0));$('coopBalance').textContent=money(cRes.data?.balance||0);$('walletUser').innerHTML='<option value="">Select member</option>'+members.filter(m=>m.status==='active').map(m=>`<option value="${m.id}">${esc(m.full_name)} · ${esc(m.member_id||'No ID')}</option>`).join('');$('memberIdUser').innerHTML='<option value="">Select member</option>'+members.filter(m=>m.status==='active').map(m=>`<option value="${m.id}">${esc(m.full_name)} · ${esc(m.member_id||'No ID')}</option>`).join('');$('membersTable').innerHTML=members.map(m=>`<tr><td>${esc(m.full_name)}</td><td><strong>${esc(m.member_id||'NOT ISSUED')}</strong></td><td>${esc(m.status)}</td><td>${esc(m.role)}</td><td>${money(bal[m.id]||0)}</td><td><button class="btn ${m.status==='active'?'red':'gray'}" data-status="${m.status==='active'?'suspended':'active'}" data-user="${m.id}">${m.status==='active'?'SUSPEND':'ACTIVATE'}</button></td></tr>`).join('')||'<tr><td colspan="6">No members.</td></tr>';const funding=fRes.data||[],qard=qRes.data||[],approved=rRes.data||[],repayments=aRes.data||[];$('fundingTable').innerHTML=funding.map(r=>`<tr><td>${esc(names[r.user_id]||r.user_id)}</td><td>${money(r.amount)}</td><td>${esc(r.payment_reference)}</td><td>${esc(r.purpose)}</td><td>${esc(r.paid_date)}</td><td class="actions"><button class="btn" data-funding="${r.id}" data-decision="approved">APPROVE</button><button class="btn red" data-funding="${r.id}" data-decision="rejected">REJECT</button></td></tr>`).join('')||'<tr><td colspan="6">No pending funding requests.</td></tr>';$('qardTable').innerHTML=qard.map(r=>`<tr><td>${esc(names[r.user_id]||r.user_id)}</td><td>${money(r.amount)}</td><td>${esc(r.repayment_plan)}</td><td>${esc(r.purpose)}</td><td class="actions"><button class="btn" data-qard="${r.id}" data-decision="approved">APPROVE</button><button class="btn red" data-qard="${r.id}" data-decision="rejected">REJECT</button></td></tr>`).join('')||'<tr><td colspan="5">No pending Qard Hasan requests.</td></tr>';$('approvedQardTable').innerHTML=approved.map(r=>`<tr><td>${esc(names[r.user_id]||r.user_id)}</td><td>${money(r.amount)}</td><td>${esc(r.purpose)}</td><td><button class="btn gold" data-disburse="${r.id}">DISBURSE</button></td></tr>`).join('')||'<tr><td colspan="4">No approved Qard waiting for disbursement.</td></tr>';$('repaymentTable').innerHTML=repayments.map(r=>`<tr><td>${esc(names[r.user_id]||r.user_id)}</td><td>${money(r.amount)}</td><td>${esc(r.payment_reference)}</td><td>${esc(r.paid_date)}</td><td class="actions"><button class="btn" data-repay="${r.id}" data-decision="approved">APPROVE</button><button class="btn red" data-repay="${r.id}" data-decision="rejected">REJECT</button></td></tr>`).join('')||'<tr><td colspan="5">No pending repayment proofs.</td></tr>';$('auditTable').innerHTML=(aRes.data||[]).map(a=>`<tr><td>${new Date(a.created_at).toLocaleString('en-NG')}</td><td>${esc(a.action)}</td><td>${esc(a.reference_id||'')}</td><td>${esc(JSON.stringify(a.details||{}))}</td></tr>`).join('')||'<tr><td colspan="4">No audit activity.</td></tr>';bindActions();}catch(e){show('globalMsg',`Dashboard error: ${e.message||e}`);}finally{loading=false;}}
function bindActions(){document.querySelectorAll('[data-status]').forEach(b=>b.onclick=async()=>{if(!confirm(`Change this member to ${b.dataset.status}?`))return;const {error}=await supabaseClient.rpc('admin_set_member_status',{p_user_id:b.dataset.user,p_status:b.dataset.status});if(error)show('globalMsg',error.message);else{show('globalMsg','Member status updated.');await load();}});document.querySelectorAll('[data-funding]').forEach(b=>b.onclick=async()=>{const note=b.dataset.decision==='rejected'?prompt('Reason for rejection (optional):')||null:null;if(!confirm(`${b.dataset.decision.toUpperCase()} this funding request?`))return;const {error}=await supabaseClient.rpc('admin_review_funding',{p_request_id:b.dataset.funding,p_decision:b.dataset.decision,p_note:note});if(error)show('globalMsg',error.message);else{show('globalMsg','Funding request reviewed.');await load();}});document.querySelectorAll('[data-qard]').forEach(b=>b.onclick=async()=>{if(!confirm(`${b.dataset.decision.toUpperCase()} this Qard Hasan request?`))return;const {error}=await supabaseClient.rpc('admin_review_qard',{p_request_id:b.dataset.qard,p_decision:b.dataset.decision});if(error)show('globalMsg',error.message);else{show('globalMsg','Qard Hasan request reviewed.');await load();}});document.querySelectorAll('[data-disburse]').forEach(b=>b.onclick=async()=>{if(!confirm('Disburse this approved Qard Hasan amount from the cooperative fund to the member wallet?'))return;const {data,error}=await supabaseClient.rpc('admin_disburse_qard',{p_request_id:b.dataset.disburse});if(error)show('globalMsg',error.message);else{show('globalMsg',`Qard disbursed successfully. Reference: ${data}`);await load();}});document.querySelectorAll('[data-repay]').forEach(b=>b.onclick=async()=>{const note=b.dataset.decision==='rejected'?prompt('Reason for rejection (optional):')||null:null;if(!confirm(`${b.dataset.decision.toUpperCase()} this repayment proof?`))return;const {error}=await supabaseClient.rpc('admin_review_qard_repayment',{p_request_id:b.dataset.repay,p_decision:b.dataset.decision,p_note:note});if(error)show('globalMsg',error.message);else{show('globalMsg','Qard repayment reviewed.');await load();}});}
$('walletForm').addEventListener('submit',async e=>{e.preventDefault();const uid=$('walletUser').value;if(!uid){show('walletMsg','Select a member.');return}const amount=Number($('walletAmount').value);if(!Number.isFinite(amount)||amount<=0){show('walletMsg','Enter a valid amount.');return}const action=$('walletAction').value;const descriptionChoice=$('walletDescription').value;const description=descriptionChoice==='Other'?$('walletCustomDescription').value.trim():descriptionChoice;if(!description){show('walletMsg','Select a reason or enter a custom description.');return}const fn=action==='credit'?'admin_credit_wallet':'admin_debit_wallet';const {error}=await supabaseClient.rpc(fn,{p_user_id:uid,p_amount:amount,p_type:'adjustment',p_description:description});if(error)show('walletMsg',error.message);else{show('walletMsg',`Wallet ${action} posted successfully.`);e.target.reset();$('walletCustomDescription').classList.add('hidden');$('walletCustomDescription').required=false;await load();}});
$('memberIdForm').addEventListener('submit',async e=>{e.preventDefault();const uid=$('memberIdUser').value;const id=$('memberIdValue').value.trim().toUpperCase();if(!uid){show('memberIdMsg','Select a member.');return}if(!/^HF-\d{3,}$/.test(id)){show('memberIdMsg','Use the format HF-001, HF-002, etc.');return}if(!confirm(`Issue member ID ${id} to this member?`))return;const {data,error}=await supabaseClient.rpc('admin_issue_member_id',{p_user_id:uid,p_member_id:id});if(error)show('memberIdMsg',error.message);else{show('memberIdMsg',`Member ID ${data} issued successfully.`);e.target.reset();await load();}});
$('logout').onclick=async()=>{await supabaseClient.auth.signOut();location.href='auth.html';};
(async()=>{currentSession=await guard();if(currentSession)await load();})();