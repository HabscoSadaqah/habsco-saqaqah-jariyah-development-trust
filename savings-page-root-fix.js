(()=>{const boot=()=>{if(!window.supabase?.createClient)return setTimeout(boot,200);const sb=window.supabase.createClient("https://ythnoeyxovapydbmymdo.supabase.co","sb_publishable_nfSR2tMCFuHCpkOjjNIakw_P85zunsN"),$=id=>document.getElementById(id),money=n=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN",minimumFractionDigits:2}).format(Number(n||0));async function load(){try{const{data:s}=await sb.auth.getSession(),uid=s?.session?.user?.id;if(!uid)return;const[{data:e},{data:a},{data:p}]=await Promise.all([sb.rpc("member_savings_loan_eligibility"),sb.from("member_cooperative_accounts").select("savings_balance").eq("user_id",uid).maybeSingle(),sb.from("member_savings_plans").select("created_at,active,frequency,fixed_amount").eq("user_id",uid).eq("active",!0).maybeSingle()]);const x=Array.isArray(e)?e[0]:e||{},balance=Number(a?.savings_balance??x.savings_balance??0),eligible=!!x.loan_eligible,since=p?.created_at||x.cycle_start_at,amount=Number(p?.fixed_amount||0),freq=(p?.frequency||"monthly").toLowerCase();let months=0,days=0;if(since){const d=new Date(since),n=new Date;if(!isNaN(d)){months=Math.max(0,Math.min(6,12*(n.getFullYear()-d.getFullYear())+n.getMonth()-d.getMonth()-(n.getDate()<d.getDate()?1:0)));const end=new Date(d);end.setMonth(end.getMonth()+6);days=Math.max(0,Math.ceil((end.getTime()-n.getTime())/864e5))}}let target=0;if(amount>0){if(freq==="daily")target=amount*Math.max(1,Math.ceil((new Date(new Date(since).setMonth(new Date(since).getMonth()+6)).getTime()-new Date(since).getTime())/864e5));else if(freq==="weekly")target=amount*Math.max(1,Math.ceil((new Date(new Date(since).setMonth(new Date(since).getMonth()+6)).getTime()-new Date(since).getTime())/(7*864e5)));else target=amount*6}const pct=eligible?100:target>0?Math.max(0,Math.min(100,balance/target*100)):0;$("savingsBalance")&&($("savingsBalance").textContent=money(balance));$("loanLimit")&&($("loanLimit").textContent=money(eligible?2*balance:2*balance));$("status")&&($("status").textContent=eligible?"Eligible":"In progress");$("months")&&($("months").textContent=months+" / 6 months");$("percent")&&($("percent").textContent=pct.toFixed(2).replace(/\.00$/,"")+"%");const bar=$("bar");bar&&(bar.style.width=pct+"%",bar.style.minWidth=pct>0?"2px":"0");$("progressText")&&($("progressText").textContent=eligible?"You have completed the six-month requirement. You may apply for an interest-free loan of up to twice your current Savings balance.":target>0?money(balance)+" saved of "+money(target)+" planned for this six-month Savings plan." :days+" day"+(days===1?"":"s")+" remaining to reach loan eligibility.");const c=$("eligibilityCta");c&&(c.style.display=eligible?"block":"none");$("eligibleAmount")&&($("eligibleAmount").textContent="Available loan limit: "+money(2*balance))}catch(err){console.error("Savings eligibility display failed",err)}}const start=()=>{load();setInterval(()=>{"visible"===document.visibilityState&&load()},1e3);window.addEventListener("habsco:savings-updated",load);document.addEventListener("visibilitychange",()=>"visible"===document.visibilityState&&load())};"loading"===document.readyState?document.addEventListener("DOMContentLoaded",start,{once:!0}):start()};boot()})();