(()=>{
'use strict';
const KEY='hf:transaction-pin-open';
let restoring=false,installed=false;
const read=()=>{try{return JSON.parse(localStorage.getItem(KEY)||sessionStorage.getItem(KEY)||'null')}catch(_){return null}};
const write=v=>{try{localStorage.setItem(KEY,JSON.stringify(v));sessionStorage.setItem(KEY,JSON.stringify(v))}catch(_){try{sessionStorage.setItem(KEY,JSON.stringify(v))}catch(__){}}};
const clear=()=>{try{localStorage.removeItem(KEY);sessionStorage.removeItem(KEY)}catch(_){}};
const isReload=()=>{try{return performance.getEntriesByType('navigation')?.[0]?.type==='reload'}catch(_){return false}};
function findPin(){return [...document.querySelectorAll('.hf-pin-modal')].find(x=>x.querySelector('#hfPinInput'))||null}
function capture(){const pin=findPin();if(!pin||pin.classList.contains('hf-pin-restored'))return;write({open:true,title:pin.querySelector('p')?.textContent||'Confirm this transaction with your PIN.'});const close=pin.querySelector('.hf-pin-close');if(close&&!close.dataset.hfRefreshBound){close.dataset.hfRefreshBound='1';close.addEventListener('click',clear,{capture:true})}}
function restore(){if(!isReload()||restoring||findPin())return;const state=read();if(!state?.open||!document.getElementById('hfSavingsModal'))return;restoring=true;const x=document.createElement('div');x.className='hf-pin-modal hf-pin-restored';x.innerHTML='<div class="hf-pin-backdrop"></div><section class="hf-pin-sheet" role="dialog" aria-modal="true"><button class="hf-pin-close" type="button">×</button><div class="hf-pin-icon">🔐</div><h3>Confirm with Transaction PIN</h3><p></p><input id="hfPinInput" inputmode="numeric" pattern="[0-9]*" maxlength="6" autocomplete="off" type="password" placeholder="Enter 6-digit PIN" aria-label="6-digit transaction PIN"><div class="hf-pin-error"></div><button class="hf-pin-confirm" type="button">Confirm</button></section></div>';x.querySelector('p').textContent=state.title||'Confirm this transaction with your PIN.';document.body.appendChild(x);const close=()=>{clear();x.remove()};x.querySelector('.hf-pin-close').onclick=close;x.querySelector('.hf-pin-backdrop').onclick=e=>e.preventDefault();x.querySelector('.hf-pin-confirm').onclick=()=>{x.querySelector('.hf-pin-error').textContent='Please close this PIN box and start the transaction again after a browser refresh.'};restoring=false;setTimeout(()=>x.querySelector('#hfPinInput')?.focus(),120)}
function reposition(){const pin=findPin(),sheet=pin?.querySelector('.hf-pin-sheet');if(!pin||!sheet)return;const base=document.getElementById('hfSavingsModal')?.querySelector('.hf-savings-sheet');if(base){const r=base.getBoundingClientRect();sheet.style.left=(r.left+r.width/2)+'px';sheet.style.top=(r.top+r.height/2)+'px'}else{sheet.style.left='50%';sheet.style.top='50%'}sheet.style.bottom='auto';sheet.style.transform='translate(-50%,-50%)'}
function run(){capture();restore();reposition();if(!installed){installed=true;const refresh=()=>requestAnimationFrame(reposition);window.addEventListener('resize',refresh,{passive:true});window.addEventListener('orientationchange',refresh,{passive:true});window.addEventListener('pageshow',refresh,{passive:true});window.visualViewport?.addEventListener('resize',refresh,{passive:true});window.visualViewport?.addEventListener('scroll',refresh,{passive:true})}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run,{once:true});else run();
new MutationObserver(()=>requestAnimationFrame(run)).observe(document.body,{childList:true,subtree:true});
})();